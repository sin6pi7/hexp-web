{
	"database": {
		"database": {
			// The elements that we are displaying on the page
			"elements": [{}],
		},
	},

	"query": {
		// All the information about querying options
		"query": {
			// The current table we are editing/viewing
			"table": "movies/tags",
			// The number of results per page
			"limit": 25,
			// The total number of results we can query for
			"results": 20,
			// The current page of results we are on
			"page": 0,
			// Special options, such as searching by name or title
			"options": {

			},
		},

		// The current status of the query
		"statusQuery": "sent/done/failed"
	}
}